<%- include('header.ejs') %>
<h2>
    shop cart
</h2>
<div>
<% for (const [key, value] of  Object.entries(cart)) {%>
   <% let totalPrice = value.qualities*value.price%>

            <h4><%=value.qualities%> <%=value.name%> total price is $<%=totalPrice%></h4>
<%} %>

</div>
<a href="/">continue shopping</a>
<%- include('root.ejs') %>